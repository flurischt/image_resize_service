import os

STORAGE_DIRECTORY = os.environ.get('IMAGE_SERVICE_STORAGE_DIR', '.')
ENABLE_DEMO = os.environ.get('IMAGE_SERVICE_ENABLE_DEMO', 'True') == 'True'
AUTH_TOKEN = os.environ.get('IMAGE_SERVICE_AUTH_TOKEN','*:token')
AUTH_TOKEN = AUTH_TOKEN.split(":") if ":" in AUTH_TOKEN else ""
AUTH_BASIC = os.environ.get('IMAGE_SERVICE_AUTH_BASIC','uploader:uploader').split(":")
AUTH_BASIC = AUTH_BASIC.split(":") if ":" in AUTH_BASIC else ""
